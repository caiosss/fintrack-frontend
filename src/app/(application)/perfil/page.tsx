import { Card, CardHeader, CardTitle } from "@/components/ui/card";

export default function PerfilPage() {

    // TODO: implementar perfil do usuário pegando os dados com um useAuth()

    return (
        <main>
            <Card>
                <CardHeader>
                    <CardTitle>Boas vindas!</CardTitle>
                </CardHeader>
            </Card>
        </main>
    );
}